import React from 'react';
import { TriageFlow } from '../../src/screens/TriageFlow';

/** Onglet « Triage » — assistant de triage du Module 1 (logique inchangée). */
export default function TriageTab() {
  return <TriageFlow />;
}
