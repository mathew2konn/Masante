/** Constantes neutres (importables partout : serveur, edge/middleware, client). */

/** Nom du cookie de session portant le token Bearer (httpOnly). */
export const SESSION_COOKIE = 'masante_session';
