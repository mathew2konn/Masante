# Architecture Decision Records — MASANTÉ

Une décision = une entrée (Rule-003). Statuts : **Accepté** (validé par le propriétaire), **Proposé**, **Perspective**.

| N° | Décision | Statut | Résumé |
|----|----------|--------|--------|
| ADR-000 | Identité de marque & palette | **Accepté** | Nom **MaSanté**, palette **Bleu Santé** + secondaire Orange. Symbole **éléphant conservé, recoloré en bleu** (logo fourni). Animation « sphères rebondissantes » au Design System P0. |
| ADR-001 | Format NIS | **Accepté** — *amendé par ADR-021* | `CIS` + année(2) + compteur(8) + checksum mod-97, vérifié client + serveur. `pays_code` par référentiel. ⚠️ **L'exemple `CIS241200012547` de cette décision est erroné** : ses deux derniers caractères n'avaient jamais été calculés. Exemple de référence corrigé : **`CIS241200012535`** (voir [ADR-021 §6](ADR-021-identifiant-national-sante.md)). |
| ADR-002 | Keycloak vs Sanctum | **Accepté** | MVP = **téléphone+OTP+Sanctum** conservés (existant validé). Keycloak/OAuth2/OIDC/MFA « prêt à activer » (module P1). |
| ADR-003 | Monorepo & moteur SQL | **Accepté** | **Monorepo pnpm** réorganisé (`apps/mobile`, `apps/web`, `packages/shared`, `services/api`) + `@masante/shared`. **MySQL conservé** en MVP ; PostgreSQL « prêt à activer » (CDC_04). |
| ADR-004 | Périmètre CQRS / Event Sourcing | Perspective | CQRS read-model sur DMEN/urgences/stats ; ES sur historique médical + audit. Non P0. |
| ADR-005 | Modèle LLM privé (CDC_07) | Perspective | Abstraction `LLMProvider`, Llama 3 cible. Hors périmètre démo. |
| ADR-006 | Clé primaire UUID v7 vs BIGSERIAL | Proposé | UUID v7 pour nouvelles tables nationales ; existant BIGINT conservé (pas de migration destructive). |
| ADR-007 | Auth téléphone+OTP, verrou, récupération | **Accepté** | Flux OTP + `VerrouContext`/PIN + `password_reset_grants` existants conservés et formalisés. |
| ADR-008 | Voies d'accès dossier (délégation + bris de glace) | **Accepté** | Existant (`Delegation`, `BrisDeGlaceService`, `AccesDossier`) conservé, aligné CDC_10 (lecture 15 min, audit, revue admin). |
| ADR-009 | Retrofit mobile vs Expo Go (MMKV) | **Accepté** | Retrofit complet du stack CDC_01 **sauf MMKV** (casse Expo Go). Cache via expo-sqlite/SecureStore ; MMKV « prêt à activer » en development build. |
| ADR-010 | Tailwind v3 (mobile+web) | **Accepté** | Pin **Tailwind 3.4** (NativeWind 4 exige v3 ; Tailwind v4 = config CSS incompatible). Preset partagé `@masante/shared/tailwind-preset`. |
| ADR-011 | Rôles & portails = Web ; mobile = patient | **Accepté** | **Mobile = app citoyenne patient uniquement** (pas de nav par rôle pro). **Tous les rôles pros + portails** (médecin, infirmier, secrétaire, pharmacien, labo, radio, admin établissement, super-admin, ministère, assurance) = **Web Next.js** (CDC_02 : Next.js 15 + Tailwind + **Shadcn UI** + TanStack + Zustand + RHF/Zod). Le **Portail Blade existant est à migrer vers Next.js**. Backend commun (API) sert les deux. |
| ADR-012 | MFA — TOTP « prêt à activer » | **Accepté** | Second facteur = **TOTP** (RFC 6238) via **`pragmarx/google2fa`** (dépendance approuvée par le propriétaire). Table `mfa_facteurs` (secret chiffré, anti-rejeu). Exigence à la connexion gouvernée par `config('mfa.enforce')` (**OFF par défaut** en MVP → login inchangé pour les patients Expo Go) ; **obligatoire** pour médecins/admin/ministère/assurances/super-admin (CDC_10 §3.5) une fois la bascule `MFA_ENFORCE=true`. Décision « MFA requis » = **backend** (`MfaService`), jamais déduite au front. SMS (via OtpService existant) dimensionné, non branché. |
| ADR-014 | [Contrôle d'intégrité interne (P5.3b-4) & rapprochement 2 sources (S11.x)](ADR-014-controle-integrite-et-rapprochement.md) | **Accepté** (interne) / Perspective (S11) | « Rapprochement » = 2 sources indépendantes ; sans passerelle réelle ni reversements §11, on livre un **auditeur d'intégrité interne** (double écriture §6.3, facture↔règlement §7.3, cashback), **détection seule**, cut-off T, runs/écarts persistés, batch + endpoint. Le vrai rapprochement 2 sources = **S11.x** : aucun code opérateur écrit, seulement un **point d'extension documenté** (format pivot settlement, point d'insertion « sources d'un run », taxonomie écarts `MONTANT_DIVERGENT`/`MANQUANT_*`/`DOUBLON`/`DECALAGE_DATE`). Classé « conçu », pas « prêt à activer ». |
| ADR-013 | Domaine Paiement = **microservice Java Spring Boot** (P5) | **Accepté** | CDC_06 §2 impose un domaine paiement **indépendant du cœur Laravel** : **Spring Boot 3.5 + PostgreSQL + Redis**, base propre, cycle de déploiement propre (`services/payment/`, hors workspace pnpm comme `services/api/`). **Exception assumée** à ADR-003 (Laravel/MySQL) : justifiée par la primauté du CDC_06 pour ce seul domaine (systèmes financiers). **Paiement SIMULÉ** (aucune passerelle Mobile Money réelle accessible — FT5) : la *structure* de domaine est correcte (interface `PaymentGateway` OCP + `SimulatedAdapter`, machine à états stricte CDC_06 §4.2 = enum `PaiementStatut` de `@masante/shared`, `Idempotency-Key` verrou Redis + unicité PG, journal d'audit **append-only à hachage chaîné**). **Frontière** : couverture CNAM/assurance, ticket modérateur et reste à charge calculés **uniquement** dans le service (jamais au front). Build/run via **Gradle wrapper + docker-compose** (JDK 25 hôte, `--release 21`). RabbitMQ/Kafka/Saga/Outbox/PCI DSS/HSM « prêts à activer » (incréments ultérieurs). Le rebranchement du flux RDV existant (Laravel = proxy, CDC_06 §10.4) est un incrément post-P5.1. |
| ADR-015 | [Cartes bancaires (P5.4a)](ADR-015-cartes-bancaires.md) | **Accepté** (simulé, FT5) | §5 carte : **frontière PCI** inviolable (jamais de PAN/CVV ; `FiltreAntiPan` Luhn→422, corps jamais loggé), **double modalité** OCP (`sim_tokenise` frictionless/3DS + `sim_redirige` redirection, zéro `if psp==`), **résultat 3DS jamais déclaré par le client** (`recupererStatut`/webhook = vérité, verrou pessimiste anti-double-capture), **webhook signé** HMAC sur corps brut avant désérialisation + fraîcheur (horodatage signé ±5 min) + anti-rejeu Redis + dédup `UNIQUE(psp,evenement_id)`, rejets **401/422 génériques** (anti-fuite). Sous-état `StatutCarte` **backend-only** projeté sur `PaiementStatut` (machine partagée jamais modifiée) ; `Montant(long,Devise)` confiné (XOF sans sous-unité). **Réconciliation à 2 sources** (registre local ⇄ vérité PSP) — vraie confrontation, distincte de l'auditeur interne ADR-014 — détection seule, idempotente. Vault « prêt §5.4 » (NTID/customer_id) mais **mandats récurrents non faits**. Prouvé G3 (build vert, 18 tests) + G2 live (11 vecteurs). |

| ADR-021 | [Identifiant National de Santé — porteur, algorithme, exposition (P6.1)](ADR-021-identifiant-national-sante.md) | **Accepté** | **Porteur = `membres_famille`** (le dossier patient ; `users` = compte, jamais porteur — sinon double identité de santé). **Auto-création du dossier titulaire en variante (c)** : à la complétion du profil, pas à l'inscription — `membres_famille` exige `date_naissance`/`sexe` que `RegisterRequest` ne collecte pas, et P1 est validé G5. Titulaire **hors quota** des 5 membres. **Algorithme MASANTE-NIS-MOD97** (ISO 7064) : préfixe converti A=10…Z=35 **inclus dans le calcul** → discrimination multi-pays sans code (`CIS…35` ≠ `SNS…04`) ; clé ∈ 02..98 ; modulo chiffre à chiffre (aucune dépendance). Détection **100 %** des erreurs d'un chiffre, des inversions voisines et des erreurs de clé. **Garde anti-divergence** : `vecteurs.json` unique consommé par la suite TS **et** PHP. **Anti-énumération** : `/nis/{nis}/verifier` ne consulte jamais la base. **Non-réutilisabilité** : `nis_journal` en `nullOnDelete`. Unicité du titulaire **déclarative** : MySQL refusant une colonne générée dérivée d'une colonne `ON DELETE CASCADE` (erreur 1215 constatée en G2), on pose colonne maintenue + `UNIQUE` + `CHECK` ; SQLite utilise un index unique partiel. Corrige l'exemple erroné d'ADR-001. |
| ADR-022 | MPI — détection **et** fusion après validation humaine (P6.2) | **Accepté** (G1) | Fusion **non destructive par pointeur logique** (`fusionne_vers_id`) plutôt que re-pointage des **18 tables** référençant `membres_famille` → défusion = remise à `NULL`, réversible par construction. Propriété du dossier fusionné traitée par **délégation croisée** sur la table `delegations` existante, sans nouveau modèle de droits. Résolution du pointeur en **un seul endroit** (`ResolveurDossier`). |
| ADR-023 | MPI — rapprochement flou sans `pg_trgm` (P6.2) | **Accepté** (G1) | MySQL 8.4 n'a ni `pg_trgm` ni Levenshtein natif, et `SOUNDEX` est calibré sur l'anglais (médiocre sur Kouassi, N'Guessan, Yao). Retenu : **clés de blocage indexées** + **scoring PHP pur** (Jaro-Winkler/Levenshtein) sur les seuls candidats bloqués. Seuils = **données** (`mpi_parametres`). Moteur **testable sans base**. |
| ADR-024 | [Référentiels nationaux — enrichissement additif (P6.3+)](ADR-024-referentiels-nationaux.md) | **Accepté** | Les tables existantes (`structures_sanitaires`, `medecins`, `medicaments`) sont **enrichies sur place** (identifiant national, district sanitaire, niveau de soins, numéro d'ordre, `pays_code`) plutôt que remplacées : P3 et P4 sont validés G5 et la règle est « corrections chirurgicales uniquement ». Deux tables pour la même chose = deux vérités, ce que §1.2.4 vient précisément résoudre. Le nom ne suit pas la nomenclature CDC_04 §121 — assumé : le contenu compte, pas l'étiquette. |
| ADR-025 | [Socle référentiel — registre, versionnage, gouvernance, diffusion (P6.3)](ADR-025-socle-referentiel.md) | **Accepté** | Étape 1 de l'ordre CDC_09 §14. **Versionnage du référentiel entier + instantané JSON** (le SCD-2 ligne à ligne aurait imposé de modifier chaque table de modules validés G5) ; **cycle §10 complet** avec **quatre-yeux** (décideur ≠ auteur) ; **anti-substitution** à la publication (contenu ré-extrait et comparé — sinon on publierait ce que personne n'a relu, et le diffusé cesserait de correspondre à la table lue par le triage) ; **diffusion à clé de cache versionnée** → aucune invalidation à écrire, mécanisme indépendant du store (Redis = changement de configuration) ; **chaîne d'audit globale** incluant `acteur_nom` (sans lui, réécrire le nom d'un agent ne rompait pas la chaîne) ; **liste blanche fermée** (`seuils_mesure`, `symptomes_triage` — les deux qui portent des règles cliniques) ; permissions `referentiel.proposer`/`.publier` **attachées à aucun rôle métier**, vérifiées en service (guard Sanctum ≠ guard `web`, piège P4). **MySQL erreur 3823** : un `CHECK` ne peut pas porter sur une colonne à action référentielle (cousin du 1215 de P6.1) → **triggers** dans les deux dialectes (CDC_04 §139), l'unicité restant déclarative. Limites L1→L7 (diffusion non branchée sur les écrans existants, estampille non branchée, < 50 ms non atteint en cache `database`, MFA OFF, pas d'interopérabilité §9, pas d'écran). **§5 : L1 et L2 sont d'une autre nature — tant qu'elles ne sont pas faites, le défaut du G0 est outillé mais pas refermé ; elles ne sont planifiées nulle part ; et L2 n'est PAS recevable avant L1** (estamper « v7 » en lisant encore la table produirait une mention fausse). Foyer pressenti : **P10** pour `symptomes_triage`, **aucun** pour `seuils_mesure`. |
| ADR-026 | [Référentiel des établissements — gouverner une projection, pas la table (P6.4a)](ADR-026-referentiel-etablissements.md) | **Accepté** | Étape 4 de l'ordre CDC_09 §14. **Décision centrale : le référentiel gouverne une PROJECTION D'IDENTITÉ ADMINISTRATIVE**, pas `structures_sanitaires` entière — `note_moyenne`/`nb_avis` sont recalculés à **chaque avis citoyen**, donc versionner la table rendrait l'instantané divergent en permanence, l'anti-substitution d'ADR-025 refuserait toute publication, et le référentiel affirmerait qu'une note d'étoiles est une donnée nationale. Exclus : téléphone, e-mail, GPS, horaires, tarifs, note. **Deux vecteurs en miroir le prouvent** (un avis ne doit PAS faire diverger ; un statut juridique DOIT). **Répond par anticipation à la limite L6 d'ADR-025** : la réponse n'est pas « l'instantané ne convient pas » mais « il ne faut pas y mettre l'état opérationnel ». Identifiant `ETS` + 6 chiffres **littéral, sans clé** (§3.2 impose un checksum pour le NIS et **pas** ici ; une clé rendrait l'exemple imposé `ETS000152` invalide) ; **le pays qualifie, il ne s'écrit pas dedans** → `UNIQUE(pays_code, identifiant)`, deux pays peuvent partager `ETS000152` ; hors `$fillable`. `type` reste la **catégorie** + `statut_juridique` en colonne propre ; ENUM élargi aux 6 valeurs manquantes du §4.1. Découpage sanitaire en **tables de référence** (§1.2.4), contrôle « le district appartient-il à la région déclarée ? ». **Tout nullable** : l'absence se dit, une valeur inventée serait *fausse* et non *manquante*. Limites M1→M5 : **onboarding Méthode 2 non livré** (CDC_11 §3 affirme le contraire — dit plutôt que tu ; foyer = portail Next), découpage **jeu partiel** (33 régions / 113 districts réels ; ne devine pas la répartition d'Abidjan faute d'arrêté), aucun écran, pas d'images, L1/L2 d'ADR-025 s'appliquent. |
| ADR-027 | [Villes couvertes et géolocalisation — la ville est un calcul, pas un `if` (P6.4b)](ADR-027-villes-geolocalisation.md) | **Accepté** | Trois villes (Abidjan, Yamoussoukro, Bouaké). **La ville est déterminée par le BACKEND** : le mobile envoie `lat`/`lng`, le serveur répond ville + communes + ordre de proximité. Si le front le calculait, ouvrir une quatrième ville exigerait de **publier une nouvelle version de l'application**, et deux versions installées répondraient différemment à la même question. **Une ville = un centre + un rayon, en données** (ajouter Korhogo = une ligne, zéro code ; Haversine PHP pur, aucune extension). **« Abidjan a des communes, les autres non » est une DONNÉE** (`affiche_communes`) — un `if ville === 'Abidjan'` serait la règle en dur que CDC_04 §20 interdit. **Communes DÉRIVÉES** des établissements (elles ne peuvent plus diverger, elles en sortent) ; promouvoir `commune` en table changerait le contrat `?commune=` de P3 validé G5 → reporté, **faute de frappe = filtre fantôme**. **Refus de localisation → choix manuel mémorisé, jamais de déduction** : le repli par adresse IP est écarté, le CGNAT ivoirien rattacherait la plupart des abonnés à Abidjan — **information fausse présentée comme certaine**. **Trois sources, trois phrases**, une seule étant une affirmation : « Vous êtes à X » (mesure) · « Ville choisie » (déclaration) · « Dernière position connue » (souvenir, hors ligne) → `localiserVille` ne passe **pas** par le cache. **Hors zone : le dire et ordonner, jamais rattacher** (un utilisateur à Man serait déclaré « à Bouaké », 300 km). **L'explication précède l'invite système**, et « Choisir moi-même » ne déclenche PAS l'invite native (le refus réflexe est exactement ce qu'on évite). G-c/G-d **corrigent la formulation initiale** : OSM ne géolocalise pas, et il n'existe aucun repli hybride après refus (une seule autorisation fusionne GPS/Wi-Fi/cellulaire ; Android 12+ « approximatif » EST l'hybride). **Correctif G-a** : les catégories avaient **4 copies déjà divergentes** → source unique `TypesEtablissement` exposée par l'API ; défaut réel révélé (`?type=centre_dialyse` répondait **422**) ; `TypeStructure` passé à `string` car **un type qui ment est pire qu'un type large**. Limites N1→N6. Aucune dépendance nouvelle. |
| ADR-028 | [Images des établissements — publiques, gouvernées par leur empreinte (P6.4c)](ADR-028-images-etablissements.md) | **Accepté** | CDC_11 §3.1 « formulaire dédié » : logo + accueil, salle d'attente, bloc opératoire, parking. **Décision propriétaire, contre la recommandation initiale : les images ENTRENT dans la projection gouvernée d'ADR-026** — et la décision se tient, la ligne d'ADR-026 séparant ce qui est *déposé délibérément par un humain identifié* de ce qui est *recalculé automatiquement* (une photo est déposée comme un numéro d'autorisation ; `note_moyenne` est recalculée à chaque avis). **Conséquence assumée : déposer une image fait diverger le référentiel** jusqu'à la publication suivante. **Ce qui entre n'est PAS le fichier** : ni les octets (l'instantané deviendrait énorme — limite L6 d'ADR-025, qu'on aggraverait), ni le chemin (un UUID : redéposer la même image ferait diverger alors que RIEN n'a changé), mais **catégorie + empreinte SHA-256 du contenu**, liste **triée** (sans tri, deux ensembles identiques donneraient deux empreintes selon l'ordre d'insertion). **Deux vecteurs en miroir** : dépôt → diverge · suppression puis redépôt du même octet pour octet → **ne diverge pas** malgré un UUID neuf. **Disque privé NON chiffré + diffusion par contrôleur** : le non-chiffrement du propriétaire est conservé (une vitrine n'a rien à protéger), le disque `public` est écarté — il exige un lien symbolique (droits admin sous Windows) et bâtit ses URL sur `APP_URL`, qui vaut l'URL Ngrok → **URL relative**, `ETag` gratuit (l'empreinte est déjà calculée), garde possible plus tard. Lecture **publique** comme le reste de l'annuaire. **Catégories ET quota en DONNÉES** (`max_par_etablissement` → « un établissement n'a qu'un logo » n'est pas un `if`) ; quota tenu **par le service sous verrou**, un déclencheur MySQL ne pouvant pas interroger la table qu'il garde (1442) — limite annoncée, pas déguisée en garantie du moteur. **Cinq gardes** dont l'habilitation à deux chemins (permission nationale **ou** gestionnaire de CET établissement — CDC_11 §3 fait remplir la vitrine par l'hôpital lui-même), vérifiée par `can()` **en service** (piège P4). Pas d'antivirus, par symétrie explicite avec `PhotoMembreService` ; liste blanche **plus étroite** que les photos de profil (HEIC exclu : publier un format qu'une partie des lecteurs ne sait pas afficher revient à publier une image invisible). **Trou réel trouvé par un vecteur de test** : `getimagesize` renvoie `[0,0]` et non `false` sur une image de zéro pixel → un PNG à dimensions nulles passait `finfo` et entrait dans le stockage public. Limites O1→O5 (aucun écran d'envoi, pas d'antivirus, pas de vignette, pas d'images hors ligne, quota applicatif). Aucune dépendance nouvelle. |
| ADR-029 | [Formulaires du portail — compléter là où ils vivent, migrer le portail en une fois (P6.4d)](ADR-029-formulaires-portail.md) | **Accepté** | Dernier incrément de P6.4 ; **referme M3, M6 (ADR-026), N5 (ADR-027) et O1 (ADR-028)**. **Question posée : Bootstrap peut-il faire un design moderne ? Techniquement oui — mais ce n'était pas la vraie question.** Le portail **Next consomme déjà** `@masante/shared/tailwind-preset` : un rendu moderne aligné sur le design system **existe déjà**. Moderniser Blade reviendrait à écrire un **second design system par-dessus Bootstrap**, en doublon, pour un portail qu'ADR-011 condamne → **l'exigence de design plaide POUR Next, pas contre**. Mais migrer les **seuls** écrans d'établissement **couperait le portail en deux** (Blade pour services/agents, Next pour l'établissement) : la migration porte sur **tout** le portail — dix-sept zones — ou sur rien. **Retenu** : compléter en Blade sans investir dans le design, et faire de **la migration un MODULE identifié**, où le design moderne se fera une fois. Coût de reprise assumé et écrit. **Le contrôle région/district passe de la DÉTECTION à l'INTERDICTION** : `exists:` accepte les deux références séparément, seule leur **combinaison** est fausse — P6.4a la détectait après coup, le formulaire l'empêche, en nommant le district et **sa vraie région**. **`forme_juridique` (lève M6) = deuxième axe** : `statut_juridique` dit **qui possède**, `forme_juridique` dit **sous quelle forme de droit** — une clinique privée peut être une SARL ou une SA ; les fondre rendrait impossible la statistique du §4.4. Texte libre car multi-pays. Les deux entrent dans la projection → **l'empreinte du référentiel change, ce n'est pas une dérive**. **`specialites` retiré du formulaire** : **ma recommandation initiale visait la mauvaise porte** — `specialites_json` était écrit et **lu par personne**, le `?specialite=` de P3 passant par `services_etablissement.specialite`, qui porte aussi **l'orientation après triage** ; poser un garde-fou dessus aurait gardé une porte que personne n'emprunte. Colonne et données **conservées** ; vraie table de référence **consignée pour P10**. **Bootstrap servi en local** : sans internet le portail s'affichait **sans aucun style**, donc inutilisable — pas cosmétique en connectivité intermittente. **Piège : `.gitignore` porte `**/vendor/` et ignorait `public/vendor/`** → le correctif aurait marché en local et **disparu ailleurs** ; déplacé en `public/assets/`, `git check-ignore` ajouté à la checklist. Formulaire d'images **séparé** (un refus de fichier ne doit pas faire perdre trente champs), gardes non réécrites (service partagé, motif P4). Limites P1→P5 : **M1 reste OUVERTE** (CDC_11 §3 affirme le contraire), design non retouché, `services.specialite` en P10, `commune` texte libre, **trois autres CDN subsistent** (html5-qrcode, Chart.js ×2). Aucune dépendance nouvelle. |
| ADR-030 | [API d'intégration — se brancher sur les systèmes existants plutôt que les recréer](ADR-030-api-integration.md) | **Conçu** (pas « prêt à activer ») | CDC_09 §9.2 · CDC_11 module 12 · **étape 9** de l'ordre §14. **Position d'architecture énoncée par le propriétaire** : MaSanté **n'a pas vocation à remplacer le SIH** d'un hôpital qui en a déjà un — là où un logiciel existe, la plateforme s'y branche (« le pharmacien n'a rien à ressaisir », CDC_11 §3.2). **Nuance sans laquelle la décision serait fausse : DEUX populations d'établissements** — sans logiciel (le **portail** leur en tient lieu, et **ce n'est pas une erreur à corriger**) et avec logiciel (l'API s'y branche) ; **aucune ne doit être imposée à l'autre** — obliger un CHU à ressaisir garantit des données fausses, obliger un cabinet à posséder un logiciel l'exclut de l'annuaire national. **Décisions** : l'API est un **contrat d'échange, jamais un second chemin d'écriture** (sinon elle devient la porte dérobée par laquelle les règles métier se contournent — un SIH qui pousse une ordonnance obéit aux mêmes gardes que P7-D0) · **le référentiel national est le PIVOT** et c'est pourquoi §14 le place avant : sans lui **il n'y a rien vers quoi mapper** ; traduction en **couche anti-corruption** (précédent ADR-019) · **trois populations d'authentification, jamais une seule étirée** — Sanctum (citoyen, existe), principal signé (interne, existe), **OAuth2 client credentials (tiers, N'EXISTE PAS** — ni Passport ni `league/oauth2`, vérifié) · **idempotence dès la conception** (un SIH sans réponse réessaie → sans clé, seconde ordonnance ; motif `Idempotency-Key` P5.1) · **webhooks signés : la CONCEPTION de P5.4a est prouvée, le code (Java, entrant) ne se réutilise pas** — le dire évite de promettre une réutilisation qui n'existe pas. **Ne résout PAS M1** : l'onboarding dit **comment un établissement entre**, l'API dit **comment ses données circulent** — une API parfaite n'attribue aucun identifiant national. **Point d'extension fixé** (domaine `integration` distinct, client rattaché par `identifiant_national`, correspondance de codes **en données** avec refus explicite d'un code inconnu, journal complet, entrant **et** sortant). **Aucune ligne de code, délibérément** : aucune API de logiciel hospitalier ivoirien n'a été vue — même classement qu'ADR-014. Limites Q1→Q5, dont **aucun partenaire réel consulté** → toute estimation de charge serait une invention. |

| ADR-031 | [Référentiel national des professionnels de santé (P6.5a)](ADR-031-referentiel-professionnels.md) | **Accepté** | CDC_09 §5.1/§5.2. **Le constat qui commande tout** : `ordonnances.medecin_nom` était un **texte libre saisi par le client**, et le rôle `medecin` n'était accepté par aucun portail — un praticien écrivait au carnet sous l'identité d'un agent d'accueil. **Projection d'identité professionnelle** : le critère d'ADR-026 est **refait, pas recopié** (`medecins` n'a aucune valeur recalculée → la ligne passe sur « qu'est-ce qui engage une autorité ? »). Entrent numéro national, profession, ordre, **autorisation d'exercer**, exercices ; sortent tarif, biographie, contacts. `PRO`+6 littéral, `UNIQUE(pays_code, numero)`, hors `$fillable`. **`professionnel.habiliter` portée par aucun rôle** : si l'employeur pouvait écrire l'autorisation, le §5.4 s'appuierait sur la déclaration de l'intéressé. **Défaut trouvé par le G2 live, pas par les tests** : le contrôle de doublon ignorait le pays → référentiel impubliable dès le 2ᵉ pays. |
| ADR-032 | [PKI et signature électronique (P6.5b)](ADR-032-pki-signature-electronique.md) | **Accepté** | CDC_09 §5.3/§5.4, CDC_10 §4. **Le serveur seul ne peut pas signer** : clé privée en AES-256-GCM dérivée d'un secret du praticien stocké **nulle part** — ce n'est pas une politique mais une propriété du code. **Ce qui est signé est le contenu EN CLAIR canonicalisé** : signer le cryptogramme aurait cassé la signature au premier rechiffrement, *et une signature qui casse toute seule n'accuse pas un coupable, elle en invente un*. Les 5 contrôles du §5.4 en **classe pure**, dans un ordre délibéré (révocation avant expiration : le motif journalisé doit être celui qui compte en litige). **Défaut trouvé par le G2** : le contrôle de révocation était **inatteignable** et mon test ne vérifiait que « ça refuse », pas « pour la bonne raison ». CA auto-signée, aucun HSM, aucun horodatage qualifié — dits sans enjoliver. |
| ADR-033 | [Référentiel national des médicaments (P6.6)](ADR-033-referentiel-medicaments.md) | **Accepté** | CDC_09 §6. **La projection prend la ligne entière** — question **reposée** et non recopiée depuis P6.4a : rien n'écrit automatiquement dans `medicaments`, les prix citoyens vivant dans `prix_pharmacie`. **Interactions = TABLE, pas colonne JSON** (une interaction est une **relation** ; deux JSON diraient deux fois la même chose et pourraient diverger sur le niveau = deux vérités sur un fait clinique). **`medicament.referentiel` distincte de `medicament.manage`** : la réutiliser aurait laissé une officine écrire les interactions du catalogue national. **P6.6b** referme le défaut central : le lien ordonnance→référentiel est **facultatif mais vérifié et figé**, sur les **trois** chemins d'écriture. Les interactions sont **consultables, jamais calculées à la prescription** (les calculer rapprocherait P6.6 d'une aide à la décision — terrain CDC_05/CDC_08). |
| ADR-034 | [Catalogue des analyses, laboratoires et valeurs de référence (P6.7)](ADR-034-catalogue-analyses.md) | **Accepté** | CDC_09 §7. **Une plage biologique dépend de la PERSONNE** : 11 g/dL est bas chez l'homme, **normal** chez la femme enceinte et chez l'enfant — une colonne min/max unique aurait affirmé le contraire pour deux d'entre eux, avec l'autorité d'une machine. D'où **une strate par population**. La classe de règles **ne conclut jamais** (vecteur dédié qui échoue si une clé de verdict apparaît). Valeurs = **jeu de démonstration**, source **obligatoire**, jamais attribuées à une autorité ; le cas des neutrophiles est inclus délibérément. **P6.7b corrige P6.7a** : la réécriture du prescripteur inscrivait le nom du **mauvais médecin** (celui qui consigne n'est pas celui qui prescrit) — retirée, remplacée par un lien vérifié ; le vecteur qui l'affirmait est **réécrit, pas supprimé**. |
| ADR-035 | [Vocabulaire national des spécialités (P6.8a)](ADR-035-referentiel-specialites.md) | **Accepté** | CDC_09 §8, **étape 8**. La spécialité vivait à **cinq endroits, en trois formes incompatibles** — dont une **constante en dur du mobile** (`don_sang`, récidive du constat G-a de P6.4b). Le « matching triage (F1.5) » promis par **trois commentaires de migration** depuis le Module 3 **n'a jamais pu aboutir** (le triage produit `ORL (Oto-Rhino-Laryngologie)`, l'annuaire compare en égalité exacte contre `orl`) ; il est **latent**, donc dangereux. **Les codes sont ADOPTÉS, jamais réinventés** → le contrat `?specialite=orl` de P3 (G5) survit intact, et la constante du mobile devient une valeur **vérifiée** au lieu d'**espérée**. **Pas d'identifiant généré** : une spécialité est un **terme de nomenclature**, pas une instance (précédent `regions.code`) ; le **code est immuable** — le renommer laisserait tous les services désigner un terme disparu sans lever d'erreur. **`nature` dit la vérité sur ce vocabulaire** : `pharmacie`, `biologie` et `don_sang` sont des **activités**, pas des spécialités médicales — le code le savait déjà sans le dire (`SPECIALITES_SANS_MEDECIN`). **De la détection à l'interdiction** : le formulaire validait `regex:/^[a-z_]+$/`, donc **le vocabulaire national était défini par ce qui avait été tapé en premier**. **`specialite.referentiel` portée par aucun rôle** (8ᵉ occurrence). **Conséquence assumée et écrite avant de coder** : le rattachement fait **changer l'empreinte du référentiel des professionnels**. **Défaut trouvé au G2, pas par les tests** : le `--dry-run` annonçait 0 praticien avant d'en rattacher 28. Limites : **l'orientation après triage reste ouverte → P10** ; contenu = adoption, pas nomenclature officielle. |
| ADR-036 | [Vaccins et calendrier vaccinal national (P6.8b)](ADR-036-calendrier-vaccinal.md) | **Accepté** | CDC_09 §8, **étape 8**. Le G0 a trouvé plus grave que le plan n'annonçait : `obligatoire` et `statut` étaient **les deux seules colonnes de `vaccinations` déclarées par le client**, et **exactement les deux que lisait la fiche vitale d'urgence** — écran montré **sans authentification** à un secouriste, sous une icône de bouclier coché. *Le bouclier couvrait une case cochée par l'intéressé lui-même.* **Un calendrier ne répond pas à « ce vaccin est-il fait ? » mais à « qu'est-ce qui est dû, pour cette personne-là, aujourd'hui ? »** (bascule d'ADR-034) → **une ligne par (vaccin, dose)**, âge en **JOURS** (un calendrier en mois ne sait pas dire « six semaines »). Le **statut devient un calcul** (classe pure, date en paramètre) et la colonne devient **nullable** — l'énoncé honnête : plus personne ne l'écrit ; l'accesseur **ne lit que sa propre ligne** (*une valeur qui change avec la façon dont on la demande n'est pas un calcul, c'est un hasard*). **La fiche vitale change de critère** et s'appuie sur `source`, signal écrit par le serveur depuis P7-D0. Le calendrier **prévient sans écrire** dans le carnet, **sans jamais nommer le vaccin**. Le consommateur neuf lit la **version publiée** dès le premier jour (503 avant publication), au lieu de relivrer le défaut que L1/L2 ont dû refermer. **`vaccin.referentiel` portée par aucun rôle** (9ᵉ occurrence). **Défaut préexistant trouvé en passant** : `update()` n'appelait pas `preparerDonnees()` — un `PUT` contournait le contrôle « ce n'est pas un laboratoire » d'ADR-034. Limites : contenu = **jeu de démonstration** (calendrier PEV officiel non vu) ; aucun rappel écrit ; lien facultatif. |

| ADR-039 | [Numéros d'urgence nationaux (P6.8e)](ADR-039-numeros-urgence.md) | **Accepté** | CDC_09 §8, **étape 8 complète**. Seul incrément de P6 dont l'exigence est **écrite mot pour mot** dans le corpus : CDC_02 §37, « rien en dur — **y compris les numéros d'urgence** ». Le G0 a trouvé **quatre familles de sites** là où le plan en annonçait deux, dont le `185` **collé dans une chaîne de traduction** de `@masante/shared` — clé **morte**, et *une traduction qui porte une donnée a l'apparence de la source unique et se périme en silence*. **Ce référentiel n'est pas comme les neuf autres** : il répond d'abord à « **que compose-t-on quand plus rien ne fonctionne ?** » — son consommateur central n'a **ni réseau, ni session, ni compte** (la carte vitale s'ouvre depuis l'écran de connexion, pour un secouriste). Les précédents pouvaient poser un **refus bruyant** avant leur v1 ; ici un refus signifierait *pas de numéro d'urgence, dans une urgence*. **Le motif n'est pas abandonné, il est DÉPLACÉ** : le **serveur** répond 503 et ne sert jamais la table de travail (honnêteté envers l'exploitant), le **client** replie sur `SecureStore` puis sur la valeur livrée (disponibilité envers le secouriste). Le repli n'est acceptable **que parce qu'il est visible** — journalisé en `warning` une fois par requête, `estEnVigueur()` ne repliant jamais, bandeau rouge au portail. **L'écran citoyen, lui, ne dit rien de la provenance** : un avertissement lu par quelqu'un qui compose des secours est du bruit au pire moment. **Un seul numéro est compilé** (le SAMU, seul que le corpus nomme) : le 100 et le 180, déclarés par le propriétaire sans arrêté, vivent dans le référentiel — les compiler referait le défaut en plus discret. Provenance **`declaration_projet`**, valeur neuve parce qu'aucune autre ne serait vraie (`autorite_nationale` affirmerait une vérification qui n'a pas eu lieu, `demonstration` dirait « inventé »). **`UNIQUE(pays_code, code)` — réponse inverse à P6.8c, pour la raison inverse** : une maladie n'appartient à aucun pays, un numéro d'urgence **n'existe que dans un plan national**. **Piège évité** : ranger les numéros dans le cache P2 les aurait fait **disparaître à la déconnexion**, précisément dans l'état du téléphone que consulte un secouriste. **`urgence.referentiel` portée par aucun rôle** (12ᵉ occurrence), et l'enjeu est le plus direct : un numéro faux ne produit pas une liste vide, **il produit un appel qui n'aboutit nulle part**. Limites : aucun des trois numéros confronté à un arrêté ; les conseils cliniques seedés gardent le numéro en dur → **P10** ; compléments du découpage hors périmètre. |
| ADR-040 | [Orientation après triage + gouvernance du triage (P10a)](ADR-040-orientation-triage.md) | **Accepté** | CDC_05 §5 + CDC_09 §10 + CDC_04 §115. **Referme T1** et la **seconde moitié du défaut du G0 de P6.3** (limite L1 d'ADR-025), dont L1+L2 avait fait l'autre le 2026-08-14. **T1 était plus grave que P6.8 ne l'annonçait** : le problème n'était pas de *forme* (libellé vs code) mais de **CARDINALITÉ** — trois des sept valeurs de `specialite_hint` portaient **deux spécialités à la fois**, qu'aucune colonne `specialite_id` n'aurait pu porter. **Trois règles médicales en dur** vivaient dans `deduireSpecialite()` (`str_contains('urgenc')`, `str_contains('gyn')`, `age < 15`) — invisibles *parce qu'elles ne ressemblent pas à des règles*, et portant sur du texte modifiable : écrire « Urgence » au singulier supprimait la priorité **sans que rien ne le signale**. Le `rang` et `sexe_requis` en font des **données gouvernées** (§10) ; la méthode est renommée **`orienter()`** — *on n'en déduit plus rien, on agrège*. **L'orientation n'est pas une déduction mais une agrégation d'annotations** (réponse à la question du propriétaire sur le mal de dent) : c'est le bon niveau, CDC_05 §1 disant que « le triage n'est jamais un diagnostic ». **Un sexe inconnu n'écarte rien** (on n'agit que sur ce qu'on sait, motif des trois silences de P7-D2) ; le repli pédiatrique ne joue **que si rien n'a été retenu** — un enfant qui a mal aux dents va chez le dentiste. **Refus bruyant avant la v1**, question **reposée** parce que P6.8e venait de ne PAS l'appliquer : son argument (consommateur sans réseau ni session) ne vaut pas pour un appel API ⇒ précédent L1+L2. La mise en vigueur devient une **étape de déploiement à deux agents**. **Seconde lecture trouvée en énumérant les lecteurs restants** : `AnalyserTriageRequest` validait `exists:symptomes,id` — la table — donc un symptôme hors version aurait été **accepté puis ignoré en silence** (constat C1 de L1+L2, mot pour mot) ; la relation `Symptome::orientations()` est **retirée** pour ne pas rouvrir la porte. Service lié en **`scoped`** : sans quoi un triage serait **jugé par une version et estampillé par une autre**. **Le libellé est FIGÉ dans l'instantané** (motif P6.6b/P7-D2) : le résoudre à la lecture exigerait deux référentiels en vigueur et laisserait un renommage changer le texte lu sans publication. **`maladies_probables_json` sort** (D5, aucun lecteur) et **`specialite_hint` aussi** — raison plus forte, elle **contredirait** `orientations` : les « deux vérités » de P6.6a. **Contrôle qualité central** : orienter vers un terme **désactivé après coup** est le seul défaut de la famille qui ne fait **aucun bruit** (la clé étrangère reste satisfaite, l'écran est vide). **La fiche §5.4 a obligé à fermer une porte ouverte depuis le Module 1** : `/triage/historique` et `/triage/{id}/fiche` étaient **publics et sans contrôle de propriété** — l'historique renvoyait les **50 derniers triages de tout le monde**, nom et symptômes compris (CDC_00 §4, interdit absolu). Le défaut n'est pas de P10a, mais **poser le QR par-dessus l'aurait aggravé** : le jeton est **le préalable** de ce que le §5.4 demande, pas un ajout de périmètre. 404 et jamais 403 — *un 403 confirmerait qu'un triage existe là*. **Ce n'est pas le jeton QR du dossier** : il garantit la **non-énumérabilité**, pas l'éphémérité. **Le QR à logo (D7) est une décision technique** : un logo efface des modules, seul le niveau **H** le tolère, et le logo est **plafonné à 20 %** — au-delà le QR *paraît* correct et cesse d'être scanné, **une panne invisible sur l'écran du développeur**. Composant unique `QrMasante` (5 sites), lisibilité réelle à prouver au G4, MFA compris. **Temps de trajet** : aucun service nouveau (OSRM depuis P3), service **`table`** = une matrice **en un seul appel** ; une durée manquante **ne retire jamais un hôpital**. Les coordonnées sont retenues **avec fraîcheur** — ADR-027 les refusait, la fraîcheur sépare l'**affirmation** « vous êtes à X » du **classement** par proximité. Limites : aucun protocole clinique (P10b/P10c) ; pas de PDF (§2.6) ; 11 symptômes sur 20 orientent ; **le libellé du repli pédiatrique est le seul non figé**, faute de porteur dans l'instantané. |

| ADR-041 | [Protocoles médicaux — registre, moteur, sélecteur, questionnaire (P10b)](ADR-041-protocoles-medicaux.md) | **Accepté** | CDC_08, **étape 4 du §13 achevée**. Le niveau de triage, le questionnaire et la borne des antécédents quittent le code : `match ($score)`, `['points_si_vrai' => 15]` et `PLAFOND_ANTECEDENTS = 20` étaient les contre-exemples littéraux du §1.2. Le registre est un **frère** du socle P6.3 et non une entrée de son registre (le §6.1 exige une version **par protocole**). Moteur en **classe pure**, trois listes blanches fermées, **`DIAGNOSTIQUER` délibérément absent** ; **un fait inconnu LÈVE**, il ne vaut pas « faux ». **On ne fabrique jamais une validation clinique** (N3) : les protocoles thérapeutiques restent des brouillons, et le §1.6 devient un comportement prouvé. b-2 : la sélection est **grossière**, le §3 est un ordre de **départage** et non de calcul, et la couverture des bandes change de **portée**. |
| ADR-042 | [Chaînes d'audit — origine déclarée et identifiants non référentiels](ADR-042-chaines-audit-origine.md) | **Accepté** | Loi 2013-450. **Le voyant rouge EST le succès** : `referentiel_journal` s'affichait « intacte » avec **97 entrées disparues** — une chaîne tronquée par la tête était indétectable, la vérification partant de `null` et acceptant n'importe quelle première entrée. **D1 : un identifiant de journal est un identifiant, pas une relation vivante** — `nullOnDelete` **dans l'empreinte** faisait crier « entrée modifiée » quand on supprimait un compte, alors que supprimer un compte est un **droit**. **D2 : une chaîne déclare son origine ET ancre sa tête** (une chaîne déclarée puis vidée et réalimentée se revérifiait « intacte » — l'accident même qu'on corrige). Aucune empreinte n'est jamais recalculée. **Le mécanisme protège à partir du jour où il est installé et ne témoigne pas du passé**, et son propre motif d'ouverture le dit. |
| ADR-043 | [Constantes cliniques du §5.2 dans le triage (P10c-1)](ADR-043-constantes-cliniques-triage.md) | **Accepté** | CDC_05 §5.1/§5.2 + CDC_08 §1.2. **Le G0 a montré que P10c ne pouvait pas commencer par l'IA** : le triage ne collecte aucune constante, et le niveau est depuis P10b-1 une **fonction déterministe du score** — un XGBoost sur les seules features disponibles **n'aurait rien appris d'autre que la règle posée à côté de lui**, avec l'apparence d'une IA et la valeur d'une tautologie. Le code avait déjà nommé le manque avec sa condition de levée (`RegistreFaitsProtocole` : « ces faits entreront quand leur collecte existera »). **Périmètre réduit en cours de route** : durée, évolution et douleur sont **des questions de protocole**, donc de la donnée, zéro code — les ajouter comme champs aurait fait un **second chemin** pour dire ce que le questionnaire dit déjà. **E1 le vocabulaire est adopté, pas réinventé** (`pouls` et non `frequence_cardiaque`, deux valeurs de tension en mmHg et non `"13/8"` à parser — *une conversion d'unité sur une tension dont l'erreur ne se verrait pas*). **E2, LE POINT DE CONCEPTION : aucun fait `constante.*_statut`** — le référentiel sait classer 39,5 °C en « critique », mais ce seuil est gouverné par **deux** signatures là où un seuil décidant de l'urgence en exige **quatre** (§7) : c'est l'asymétrie que P10b-3-i a passé un incrément à refermer, on ne la rouvre pas un cran plus bas. Le référentiel fournit l'**unité, les décimales et la plausibilité** (*300 °C n'est pas un patient*) ; le seuil qui change un triage est une **règle signée** ⇒ **le §1.2 retourné à l'endroit**. **E3 le carnet propose, le patient confirme** : `fraicheur_max_minutes` **en donnée par type** (une saturation vaut une heure, un poids des mois), hors fenêtre la valeur est **montrée sans être pré-remplie** — *une température prise il y a trois mois n'est pas une température* (les trois sources de P6.4b). **E4 le triage n'écrit rien dans le carnet** (4ᵉ chemin d'écriture refusé, motif P6.8b) et `mesure_id` est un **identifiant, pas une clé étrangère** (ADR-042 D1). **E5 on refuse, on n'écrête jamais** — y compris une décimale de trop. **Le G2 live y a trouvé le défaut que 46 vecteurs ne voyaient pas** : le référentiel publiait `decimales = 1` et le serveur acceptait `39,55` — la borne gouvernée était **décorative**, seule la capacité de la colonne mordait ; tous les vecteurs éprouvaient `39,555`, valeur que *les deux* bornes refusent, et *il fallait celle qui les sépare*. Forme exacte du constat X4 de P10b-3-i, laissée ouverte un cran plus loin. **E6 l'origine est décidée par le serveur**. **Conséquence : cinquième étape de déploiement, et elle passe en premier** — 18 vecteurs sont tombés d'un coup, dont six qui refusaient désormais **pour le mauvais motif**. **Mutation : `g5` a d'abord SURVÉCU** — le vecteur prouvait l'arithmétique (`(int) null` vaut 0) et non l'intention ; le cas qui les sépare est une mesure **datée du futur** (7ᵉ instance de cette famille). Limites : **aucune IA** (P10c-2) ; aucune allergie structurée ; fenêtres de fraîcheur = **jeu de démonstration**. |

> Détail complet et alternatives : voir le Knowledge Book et l'historique de conversation de la bascule v2.0 (2026-07-31).
