# triage-service — socle P10c-2-i (CDC_05 §5.5.4). AUCUNE dépendance ML : ce service n'a aucun
# modèle à charger aujourd'hui (F5 du G1 P10c2i) ; XGBoost/SHAP/MLflow arriveront en P10c-3, sous
# accord écrit (§2.6), avec le modèle qui les justifiera. Versions bornées pour la reproductibilité.
fastapi>=0.111,<0.116
uvicorn[standard]>=0.30,<0.35
pydantic>=2.7,<3
pydantic-settings>=2.3,<3

# Qualité (G3)
pytest>=8.2,<9
ruff>=0.5,<0.9
mypy>=1.10,<2
httpx>=0.27,<0.29
